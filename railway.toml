[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd backend && chmod +x mvnw && ./mvnw spring-boot:run"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[[services]]
name = "smart-campus-backend"
source = "."

[services.variables]
JAVA_VERSION = "21"
PORT = "8082"
